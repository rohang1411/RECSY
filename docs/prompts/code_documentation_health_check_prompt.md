Can you go through the entire project in detail, understand the current state of the project, see how it differs from @docs/RECSY_V2_PROJECT_GUIDE.md and @docs/RECSY_V2_PROJECT_CONTEXT.md documents.

Make sure that everything is well updated in @docs/RECSY_V2_PROJECT_GUIDE.md and @docs/RECSY_V2_PROJECT_CONTEXT.md . The context document should cover every single thing, all the issues encountered (you can check the previous commits on the master branch if you want more information) and how we solved them, their commit numbers should be hyperlinked inline for reference. The issues should be sorted by severity. Add a section (might be present already) in which we'll mention all the rules we follow while developing - like the documents have to be updated as the development is going on (ADR, context doc, guide, implementation plans etc.., status should be tracked accross all documents), tests should be written for everything, all the code and files should be properly commented, proper logs should be added everywhere in the code. These things must be follow by a human aur AI agent while developing the code.

Keep the @docs/RECSY_V2_PROJECT_GUIDE.md document light for getting a basic understanding of the project of what's the goal and how everything works. The @docs/RECSY_V2_PROJECT_CONTEXT.md document should be highly detailed and contain every single thing about the project in depth including the gaps and future tasks, usecases,etc.. If some code needs to be refactored or something needs to be changed then add a pending tasks section in the @docs/RECSY_V2_PROJECT_CONTEXT.md document and mention everything there. Make sure that the ADR documents are correct and there are ADR documents for everything that's required. Also update the README file properly and make it look better like a professional repository. Keep the V1 part of the project as well at the bottom. Make sure the test cases are present for everything. If anything is missing add it. Make sure that every file is properly commented, each file has a comment in the begining, that states the purpose of this file, what this file has, how its implemented and its use.

The goal is to check that the documentation, code quality and everything is perfect as we go ahead and add new features. Make sure that you don't make any mistakes or errors. Also plan first and then update so that you don't do any rework and use less tokens.
