# --- Node / Next.js -----------------------------------------------------------
node_modules
.pnpm-store

.next/
out/
build/

# --- Env ----------------------------------------------------------------------
.env
.env.*
!.env.example

# --- Testing ------------------------------------------------------------------
coverage/

# --- Misc ---------------------------------------------------------------------
.DS_Store
*.pem
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Vercel -------------------------------------------------------------------
.vercel

# --- TypeScript ---------------------------------------------------------------
*.tsbuildinfo
next-env.d.ts

# --- Drizzle ------------------------------------------------------------------
drizzle/meta/

# --- Legacy (Flutter) build artifacts ----------------------------------------
# The `legacy/` directory preserves the pre-rewrite Flutter project for
# reference. We keep the source tracked but ignore generated output to avoid
# bloating the repo.
legacy/.dart_tool/
legacy/build/
legacy/.flutter-plugins-dependencies
legacy/**/.last_build_id
legacy/**/Pods/
legacy/**/GeneratedPluginRegistrant.*
legacy/**/.gradle/
legacy/**/local.properties
legacy/**/key.properties
legacy/**/*.iml
legacy/**/xcuserdata/
legacy/**/Flutter/Flutter.framework
legacy/**/Flutter/Flutter.podspec
legacy/**/DerivedData/
